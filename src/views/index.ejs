<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Nodejs Mysql CRUD</title>
    <!-- NAVIGATION -->
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css"/>
  </head>
  <body>
    <!-- navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="/">BATALLA DE CONOCIMIENTOS 8 "ÚLTIMO JEDI"</a>
    </nav>
    <div class="container mt-5">
      <div class="h2">Lista de Servidores</div>
      <div class="row">
        <div class="col-md-7">
          <table class="table table-bordered table-hover">
            <thead>
              <tr>
                <td>Hostname</td>
                <td>Dirección IP</td>
                <td>Máscara de Subred</td>
                <td>Gateway</td>
              </tr>
            </thead>
            <tbody>
              <% if (data) { %> <% for(var i = 0; i < data.length; i++) { %>
              <tr>
                <td><%= data[i].hostname %></td>
                <td><%= data[i].direccion_ip %></td>
                <td><%= data[i].netmask %></td>
                <td><%= data[i].gateway %></td>
              </tr>
              <% } %> <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </body>
</html>
